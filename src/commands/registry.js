'use strict';

var CMD = require('./structure');

module.exports = {
  help: CMD.HELP,
  enter: CMD.ENTER,
  rooms: CMD.ROOMS,
  servers: CMD.SERVERS,
  nick: CMD.NICK,
  create: CMD.CREATE,
  join: CMD.JOIN,
  chat: CMD.CHAT,
  me: CMD.ME,
  msg: CMD.MSG,
  leave: CMD.LEAVE,
  quit: CMD.QUIT
}
